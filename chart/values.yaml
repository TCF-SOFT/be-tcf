# Default values for chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: dev
app: be-tcf
host: api-tcf.eucalytics.uk

datadog:
  env: dev
  service: be-tcf

deployment:
  name: be-tcf-deployment
  replicas: 1

image:
  repository: ghcr.io/utikpuhlik/be-tcf
  pullPolicy: Always
  tag: latest

service:
  type: ClusterIP
  name: be-tcf-svc
  port: 8080

resources:
  requests:
    memory: '384Mi'
    cpu: '100m'
  limits:
    memory: '512Mi'
    cpu: '200m'

secrets:
#  aws: aws-secret
  api_utils: api-utils-secret
  home_bar: be-tcf-secret

imagePullSecrets:
  name: ghcr-secret

ingress:
  enabled: true
  name: be-tcf-ingressroute
  className: traefik
  host: api-tcf.eucalytics.uk
  annotations: { }


certificate:
  name: be-tcf-ingressroute-certificate
  secretName: be-tcf-tls-secret
  clusterIssuer: cloudflare-clusterissuer

replicaCount: 1

#livenessProbe:
#  httpGet:
#    path: /
#    port: http
#readinessProbe:
#  httpGet:
#    path: /
#    port: http

# autoscaling:
#   enabled: false
#   minReplicas: 1
#   maxReplicas: 5
#   targetCPUUtilizationPercentage: 50
  # targetMemoryUtilizationPercentage: 80
